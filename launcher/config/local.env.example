# UDO Development Platform - Local 모드 환경변수
# 이 파일을 .env로 복사하여 사용하세요
# Docker 없이 로컬에서 개발할 때 사용

# ============================================================================
# 데이터베이스 설정 (Local 모드)
# ============================================================================

# SQLite 사용 (PostgreSQL 불필요)
DATABASE_URL=sqlite:///./udo_local.db

# PostgreSQL 사용 시 (선택)
# DATABASE_URL=postgresql://udo_dev:your_password@localhost:5432/udo_v3

# ============================================================================
# 캐시 설정
# ============================================================================

# Redis 비활성화 (Local 모드 기본값)
DISABLE_REDIS=true

# Redis 사용 시 (선택)
# REDIS_URL=redis://localhost:6379/0

# ============================================================================
# 애플리케이션 설정
# ============================================================================

# 환경 (development, staging, production)
ENVIRONMENT=development

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=DEBUG

# 개발 모드 인증 우회 (개발 환경에서만 true)
DISABLE_AUTH_IN_DEV=true

# ============================================================================
# 프론트엔드 설정
# ============================================================================

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# ============================================================================
# AI 서비스 (선택)
# ============================================================================

# AI 기능을 사용하려면 아래 키를 설정하세요
# OPENAI_API_KEY=sk-your-openai-key
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key
# GEMINI_API_KEY=your-gemini-key

# ============================================================================
# Obsidian 연동 (선택)
# ============================================================================

# Obsidian Vault 경로 (개발 노트 동기화용)
# OBSIDIAN_VAULT_PATH=C:\Users\user\Documents\Obsidian Vault

# ============================================================================
# 보안 설정 (개발 환경)
# ============================================================================

# JWT 비밀 키 (개발용 - Production에서는 반드시 변경)
JWT_SECRET_KEY=dev-secret-key-change-in-production

# 세션 비밀 키
SESSION_SECRET=dev-session-secret-change-in-production
